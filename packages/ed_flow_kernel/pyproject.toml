[project]
name = "ed-flow-kernel"
version = "0.4.0"
description = "UI-independent capability kernel for AHS ED Flow Action Intelligence."
requires-python = ">=3.11"
dependencies = [
  "pandas>=2.2",
  "numpy>=1.26",
  "scikit-learn>=1.4",
  "pydantic>=2.7",
  "PyYAML>=6.0",
]

[tool.pytest.ini_options]
pythonpath = ["../../src", "."]
